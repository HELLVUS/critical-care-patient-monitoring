# 🏥 Система мониторинга пациентов в критическом состоянии (Critical Care Patient Monitoring)

[![Python](https://img.shields.io/badge/Python-3.7+-blue.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Status](https://img.shields.io/badge/Status-Active-success.svg)](https://github.com/yourusername/critical-care-patient-monitoring)

## 📋 Описание проекта

Система мониторинга жизненных показателей пациентов в отделении реанимации с применением искусственного интеллекта для обеспечения информационной безопасности. Проект демонстрирует комплексный подход к управлению инцидентами информационной безопасности в медицинских интеллектуальных системах.

### Основная тема
**Информационная безопасность интеллектуальных систем**  
**Предмет:** Управление инцидентами ИБ с применением ИИ

---

## 🎯 Ключевые возможности

### Триада безопасности CIA

#### 🔒 **C - Конфиденциальность (Confidentiality)**
- **RBAC (Role-Based Access Control)** — ролевая модель доступа с политиками для врачей, медсестер, администраторов и аналитиков
- **UEBA (User and Entity Behavior Analytics)** — анализ поведения пользователей для выявления подозрительной активности
- **AES-GCM шифрование** — защита данных пациентов с использованием современного алгоритма шифрования
- **Анонимизация данных** — псевдонимизация идентификаторов пациентов с использованием SHA-256

#### ✅ **I - Целостность (Integrity)**
- **Валидация физиологических лимитов** — проверка жизненных показателей на соответствие медицинским нормам
- **Анализ скорости изменения** — обнаружение аномальных скачков показателей (rate-of-change)
- **Кросс-датчиковая валидация** — сравнение данных от разных датчиков (ECG vs SpO2) для выявления ошибок
- **Анализ трендов с EWMA** — обнаружение отклонений от базовой линии с использованием экспоненциально взвешенного скользящего среднего

#### ⚡ **A - Доступность (Availability)**
- **Многоуровневое кеширование** — трехуровневая архитектура кеша:
  - **L1:** Кеш на мониторе пациента (последние 10 записей)
  - **L2:** Кеш в шлюзе отделения (до 1000 записей, TTL 60 минут, LRU)
  - **L3:** Центральный сервер (полная история, без TTL)
- **Отказоустойчивость** — система продолжает работу при падении центрального сервера

---

## 🏗️ Архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│              СИСТЕМА МОНИТОРИНГА ПАЦИЕНТОВ                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐ │
│  │  ДАТЧИКИ     │───▶│  ВАЛИДАЦИЯ   │───▶│   КЕШИРОВАНИЕ│ │
│  │  (ECG, SpO2, │    │  ЦЕЛОСТНОСТИ │    │   (L1/L2/L3) │ │
│  │   BP, RR)    │    │              │    │              │ │
│  └──────────────┘    └──────────────┘    └──────────────┘ │
│         │                    │                    │         │
│         └────────────────────┼────────────────────┘         │
│                              │                              │
│                    ┌─────────▼─────────┐                   │
│                    │  RBAC + UEBA      │                   │
│                    │  ШИФРОВАНИЕ       │                   │
│                    │  АНОНИМИЗАЦИЯ     │                   │
│                    └───────────────────┘                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 Установка и требования

### Системные требования
- Python 3.7 или выше
- Операционная система: Windows, Linux, macOS

### Установка зависимостей

```bash
# Клонирование репозитория
git clone https://github.com/yourusername/critical-care-patient-monitoring.git
cd critical-care-patient-monitoring

# Установка зависимостей
pip install cryptography matplotlib
```

### Минимальные зависимости
- `cryptography` — для AES-GCM шифрования
- `matplotlib` — для генерации графиков (опционально)

---

## 🚀 Быстрый старт

### Запуск демонстрации

```bash
python patient_monitoring_system.py
```

Скрипт выполнит полную демонстрацию системы:
1. Генерацию синтетических данных для 3 пациентов
2. Обработку данных с проверкой целостности
3. Симуляцию падения и восстановления сервера
4. Генерацию подозрительных доступов для демонстрации UEBA
5. Создание графиков и отчетов

### Результаты работы

После выполнения скрипта в папке `результаты/` будут созданы следующие графики:

- `fig1_vitals_trend_PAT003.png` — тренд пульса пациента с отметками аномалий
- `fig2_integrity_alert_counts.png` — количество алертов целостности по типам
- `fig3_cache_levels_over_time.png` — динамика заполнения кешей по уровням
- `fig4_accesses_per_hour.png` — сравнение нормальных и подозрительных доступов
- `fig5_distinct_patients_burst.png` — обнаружение массового доступа

---

## 📖 Использование API

### Базовый пример

```python
from patient_monitoring_system import (
    PatientMonitoringSystem,
    PatientVitals,
    User
)
from datetime import datetime

# Инициализация системы
system = PatientMonitoringSystem()

# Создание пользователя
doctor = User(
    user_id='DR_IVANOV',
    role='doctor',
    assigned_patients=['PAT_001', 'PAT_003']
)
system.confidentiality_manager.add_user(doctor)

# Создание жизненных показателей
vitals = PatientVitals(
    patient_id='PAT_001',
    timestamp=datetime.now(),
    heart_rate=75,
    systolic_bp=120,
    diastolic_bp=80,
    oxygen_saturation=98.5,
    respiratory_rate=16,
    heart_rate_spo2=76  # HR из SpO2 датчика
)

# Обработка данных
result = system.process_patient_data(
    vitals=vitals,
    user_id='DR_IVANOV',
    location='127.0.0.1',
    user_role='doctor',
    assigned_patients=['PAT_001', 'PAT_003']
)

# Проверка результатов
print(f"Алерты целостности: {len(result['integrity_alerts'])}")
print(f"Доступ разрешен: {result['confidentiality_check'].get('access_allowed', False)}")
```

### Проверка целостности данных

```python
from patient_monitoring_system import IntegrityValidator, PatientVitals

validator = IntegrityValidator()

# Валидация жизненных показателей
vitals = PatientVitals(...)
alerts = validator.validate_all(vitals)

for alert in alerts:
    print(f"[{alert.severity}] {alert.alert_type}: {alert.details}")
```

### Работа с кешированием

```python
from patient_monitoring_system import MultiLevelCache, PatientVitals

cache = MultiLevelCache()

# Запись данных
vitals = PatientVitals(...)
cache.write(vitals)

# Чтение данных (автоматический выбор уровня кеша)
latest_vitals = cache.read('PAT_001')

# Статистика кешей
stats = cache.get_cache_stats()
print(f"L1 записей: {stats['level1_total_records']}")
print(f"L2 записей: {stats['level2_records']}")
print(f"L3 записей: {stats['level3_records']}")
```

### Управление конфиденциальностью

```python
from patient_monitoring_system import ConfidentialityManager

conf_manager = ConfidentialityManager()

# Шифрование записи
record_dict = vitals.to_dict()
encrypted = conf_manager.encrypt_record(record_dict)

# Расшифровка
decrypted = conf_manager.decrypt_record(encrypted)

# Анонимизация данных
anonymized_records = conf_manager.anonymize_records([record_dict])

# Экспорт логов доступа
conf_manager.export_access_logs_to_csv('access_logs.csv')
```

---

## 🔧 Конфигурация

### Настройка порогов целостности

```python
INTEGRITY_CONFIG = {
    'heart_rate_min': 40,
    'heart_rate_max': 250,
    'systolic_bp_max': 240,
    'diastolic_bp_max': 160,
    'oxygen_saturation_min': 50,
    'oxygen_saturation_max': 100,
    'rate_of_change_hr_threshold': 30,  # ударов/мин за минуту
    'rate_of_change_o2_threshold': 5,   # процентов за минуту
    'rate_of_change_bp_threshold': 20,  # мм рт.ст. за минуту
    'cross_sensor_n_samples': 5,        # N последовательных измерений
    'ewma_alpha': 0.3,                  # коэффициент сглаживания
    'ewma_k_sigma': 2.5                 # k стандартных отклонений
}
```

### Настройка кеширования

```python
CACHE_CONFIG = {
    'level1_max_records': 10,        # На мониторе пациента
    'level2_max_records': 1000,       # В шлюзе отделения
    'level2_ttl_minutes': 60,        # TTL для уровня 2
    'level3_ttl_minutes': None        # Без TTL для центрального сервера
}
```

### Настройка UEBA

```python
UEBA_CONFIG = {
    'max_patients_per_session': 10,
    'max_accesses_per_hour': 50,
    'working_hours': (6, 22),
    'mass_access_threshold': 15,           # Разных пациентов за T минут
    'mass_access_window_minutes': 10
}
```

---

## 👥 Роли пользователей (RBAC)

### Врач (Doctor)
- ✅ Доступ к назначенным пациентам
- ✅ Возможность расшифровки данных
- ❌ Доступ к другим пациентам отделения
- ❌ Управление пользователями

### Медсестра (Nurse)
- ✅ Доступ ко всем пациентам отделения
- ✅ Возможность расшифровки данных
- ❌ Доступ к назначенным конкретным пациентам
- ❌ Управление пользователями

### Администратор (Admin)
- ✅ Управление пользователями
- ❌ Чтение медицинских данных пациентов
- ❌ Расшифровка данных

### Аналитик (Analyst)
- ✅ Работа с анонимизированными данными
- ❌ Доступ к персональным данным
- ❌ Расшифровка записей

---

## 🛡️ Механизмы безопасности

### 1. Валидация целостности данных

#### Физиологические лимиты
Система проверяет жизненные показатели на соответствие медицинским нормам:
- Пульс: 40-250 уд/мин (≥250 считается ошибкой датчика)
- Систолическое давление: ≤240 мм рт.ст.
- Диастолическое давление: ≤160 мм рт.ст.
- SpO2: 50-100%

#### Анализ скорости изменения
Обнаружение аномальных скачков показателей:
- Пульс: порог 30 уд/мин за минуту
- SpO2: порог 5% за минуту
- Давление: порог 20 мм рт.ст. за минуту

#### Кросс-датчиковая валидация
Сравнение данных от разных датчиков (например, HR из ECG vs HR из SpO2) для выявления расхождений. Требуется N последовательных измерений для снижения ложных срабатываний.

#### Анализ трендов (EWMA)
Вычисление экспоненциально взвешенного скользящего среднего для базовой линии и флаг отклонений за k сигм.

### 2. Многоуровневое кеширование

```
┌─────────────────┐
│   Монитор (L1)  │ ← Последние 10 записей
└────────┬────────┘
         │
┌────────▼────────┐
│  Шлюз (L2)      │ ← До 1000 записей, TTL 60 мин, LRU
└────────┬────────┘
         │
┌────────▼────────┐
│  Сервер (L3)    │ ← Полная история, без TTL
└─────────────────┘
```

### 3. UEBA (User and Entity Behavior Analytics)

Система анализирует поведение пользователей и выявляет:
- **Массовый доступ** — много разных пациентов за короткое время
- **Необычное время** — доступы вне рабочего времени (6:00-22:00)
- **Необычное место** — доступы из неизвестных IP-адресов

### 4. Шифрование и анонимизация

- **AES-GCM шифрование** — защита данных с использованием 256-битного ключа
- **HMAC-SHA256** — подпись записей для проверки целостности
- **Псевдонимизация** — замена идентификаторов пациентов на хеши SHA-256

---

## 📊 Примеры использования

### Обнаружение ошибки датчика

```python
# Генерация данных с ошибкой датчика (пульс = 260)
vitals = PatientVitals(
    patient_id='PAT_002',
    timestamp=datetime.now(),
    heart_rate=260,  # Невозможное значение
    systolic_bp=120,
    diastolic_bp=80,
    oxygen_saturation=98.0,
    respiratory_rate=16
)

alerts = validator.validate_all(vitals)
# Результат: критический алерт "Пульс >= 250 - ошибка датчика"
```

### Выявление подозрительного доступа

```python
# Массовый доступ к 19 пациентам за 10 минут
system.confidentiality_manager.log_access(
    user_id='SUSPICIOUS_USER',
    accessed_patients=[f'PAT_{i:03d}' for i in range(1, 20)],
    timestamp=datetime.now(),
    location='UNKNOWN_IP',
    action='view'
)
# Результат: UEBA алерт "Массовый доступ: 19 разных пациентов"
```

### Работа при падении сервера

```python
# Симуляция падения центрального сервера
cache.simulate_server_failure()

# Система продолжает работать через L1 и L2 кеши
vitals = PatientVitals(...)
cache.write(vitals)  # Данные сохраняются в L1 и L2

# Восстановление сервера
cache.simulate_server_recovery()
```

---

## 📈 Визуализация данных

Система автоматически генерирует графики для анализа:

1. **Тренды жизненных показателей** — визуализация изменений показателей с отметками аномалий
2. **Статистика алертов** — распределение алертов целостности по типам
3. **Динамика кешей** — изменение количества записей в кешах по уровням во времени
4. **Анализ доступов** — сравнение нормальных и подозрительных доступов по часам
5. **Массовый доступ** — обнаружение всплесков доступа к разным пациентам

---

## 🧪 Тестирование

### Запуск демонстрации с полным отчетом

```bash
python patient_monitoring_system.py
```

Демонстрация включает:
- ✅ Обработку 180+ записей для 3 пациентов
- ✅ Симуляцию различных типов аномалий
- ✅ Проверку работы RBAC и UEBA
- ✅ Тестирование отказоустойчивости
- ✅ Генерацию графиков и отчетов

---

## 📝 Структура проекта

```
critical-care-patient-monitoring/
│
├── patient_monitoring_system.py  # Основной модуль системы
├── README.md                     # Документация проекта
│
└── результаты/                   # Папка с результатами
    ├── fig1_vitals_trend_PAT003.png
    ├── fig2_integrity_alert_counts.png
    ├── fig3_cache_levels_over_time.png
    ├── fig4_accesses_per_hour.png
    └── fig5_distinct_patients_burst.png
```

---

## 🔬 Технические детали

### Алгоритмы и методы

- **EWMA (Exponentially Weighted Moving Average)** — для анализа трендов
- **LRU (Least Recently Used)** — алгоритм вытеснения в кеше L2
- **TTL (Time To Live)** — время жизни записей в кеше L2
- **HMAC-SHA256** — для цифровых подписей
- **AES-GCM** — для симметричного шифрования

### Производительность

- Обработка данных в реальном времени
- Минимальная задержка при валидации (< 10 мс)
- Эффективное использование памяти благодаря ограничению размеров кешей
- Отказоустойчивость при падении центрального сервера

---

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста:

1. Создайте форк репозитория
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

---

## 👨‍💻 Автор

**Васильков Алексей**  
Группа: БСМО-12-25  
Год: 2025

---

## 🙏 Благодарности

- Преподавателям курса "Информационная безопасность интеллектуальных систем"
- Разработчикам библиотек `cryptography` и `matplotlib`
- Сообществу Python за отличные инструменты и документацию

---

## 📚 Дополнительные ресурсы

- [Документация Python](https://docs.python.org/3/)
- [Cryptography Library](https://cryptography.io/)
- [Matplotlib Documentation](https://matplotlib.org/)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/) — рекомендации по безопасности

---

## ⚠️ Важные замечания

- ⚠️ **Это демонстрационный проект** — не используйте в продакшене без дополнительной проверки безопасности
- ⚠️ **Ключи шифрования** — в реальной системе должны храниться в безопасном хранилище (HSM, Key Vault)
- ⚠️ **Медицинские данные** — соблюдайте требования HIPAA, GDPR и других регуляций при работе с реальными данными
- ⚠️ **Аутентификация** — в демо-версии упрощена, в продакшене требуется полноценная система аутентификации

---

## 🐛 Известные ограничения

- Система использует синтетические данные для демонстрации
- Аутентификация пользователей упрощена (нет реальных токенов/сессий)
- Ключи шифрования генерируются случайно при каждом запуске
- Нет интеграции с реальными медицинскими датчиками

---

## 🔮 Планы на будущее

- [ ] Интеграция с реальными медицинскими датчиками
- [ ] Веб-интерфейс для визуализации данных
- [ ] REST API для интеграции с другими системами
- [ ] Поддержка машинного обучения для предсказания критических состояний
- [ ] Расширенная система аутентификации (OAuth2, JWT)
- [ ] Интеграция с блокчейном для аудита доступа
- [ ] Поддержка распределенной архитектуры (микросервисы)

---

## 📞 Контакты

Если у вас есть вопросы или предложения, пожалуйста, создайте Issue в репозитории.

---

**⭐ Если проект был полезен, поставьте звезду!**
